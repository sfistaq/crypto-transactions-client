import styled from "styled-components";
import {
  ButtonGroup,
  ButtonGroupProps,
  Button,
  ButtonProps,
} from "@mui/material";

export const ToggleWrapper = styled(ButtonGroup)<ButtonGroupProps>`
  margin-bottom: 20px;
`;

export const ToggleButton = styled(Button)<ButtonProps>``;
